# Add Spec

Follow the complete workflow in `commands/add-spec.md`.

Use `$ARGUMENTS` as the feature description. Read `CLAUDE.md`, inspect the existing project, establish product behavior before technical design, and create `specs/<feature>/requirements.md`, `design.md`, and `tasks.md` with stage-specific readiness.

Do not implement application code during this command.
