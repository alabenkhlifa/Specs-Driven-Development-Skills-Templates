# Add Spec

Follow the complete workflow in `commands/add-spec.md`.

Use `$ARGUMENTS` as the feature description. Read `CLAUDE.md`, inspect the existing project, and create `specs/<feature>/requirements.md`, `design.md`, and `tasks.md` only after blocking decisions are resolved.

Do not implement application code during this command.

