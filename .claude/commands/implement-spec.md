# Implement Spec

Follow the complete workflow in `commands/implement-spec.md`.

Use `$ARGUMENTS` as the specification path or feature name. Implement only the active approved slice, respect every stop condition, run the complete verification gate, and write progress back to `tasks.md`.

