# Review Spec

Follow the complete workflow in `commands/review-spec.md`.

Use `$ARGUMENTS` to identify the specification and implemented slice. Read `CLAUDE.md`, inspect the working tree, re-run each completed task's proof and the verification gate, and record an evidence-based review checkpoint.

Do not implement fixes or change the specification agreement during this command.
