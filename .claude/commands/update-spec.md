# Update Spec

Follow the complete workflow in `commands/update-spec.md`.

Use `$ARGUMENTS` as the specification path and requested decision change. Keep requirements, design, active tasks, release gates, and verification aligned.

Do not implement application code during this command.
